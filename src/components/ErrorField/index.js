import React from 'react';
import { Alert } from 'react-native';
import {useNetInfo} from "@react-native-community/netinfo";

function Toast({navigation}) {

  

  return (
    <></>    
  );
}

export default Toast;